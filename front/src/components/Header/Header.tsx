import { Navigation } from "../Navigation/Navigation"

export const Header = () => {
    return (
        <header>
            <div className="container">
                <Navigation />
            </div>
        </header>
    )
} 